<section class="cta">
  <div class="shell cta-in rv">
    <div>
      <h2><%= cta.h %></h2>
      <p><%= cta.p %></p>
    </div>
    <div class="cta-btns">
      <a class="btn btn-white" href="/contact">Request a Quote</a>
      <a class="btn btn-ghost" href="<%= site.phoneHref %>" data-evt="call">Call <%= site.phone %></a>
    </div>
  </div>
</section>
